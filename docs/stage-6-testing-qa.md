# Этап 6: Тестирование и QA

**Статус**: ⏳ Запланирован

## Цель этапа

Полное покрытие приложения тестами: unit (backend Jest, frontend Vitest+RTL) и E2E (Playwright). Цель — >80% покрытия, все проверки зелёные.

## Темы Claude Code для практики

- **Dynamic workflows** (инструмент Workflow) — параллельная генерация unit-тестов по многим файлам без покрытия, с взаимной проверкой результата: pipeline «сгенерировать тест → верифицировать другим агентом, что тест осмысленный и реально проходит» (а не tautological-заглушка).
- **Agent view** — мониторинг нескольких параллельных фоновых прогонов тестов одним взглядом.
- **`/loop`** — итеративный цикл «прогнать тесты → пофиксить падения → повторить» до зелёного билда.
- **MCP Playwright/Chrome** — для E2E-сценариев.

## Что реализуем

### Backend (Jest)
- `LinksService`: CRUD-логика, генерация и уникальность `shortCode`, edge cases (несуществующий id, конфликт custom-alias).
- `RedirectController`: `404` для несуществующего кода, `410` для истёкшей/неактивной ссылки, `302` с верным `Location`.
- `AnalyticsService`: агрегация — с моками `PrismaService` (groupBy-результаты, проверка корректности `clicksByDay` за пустые дни).
- `AuthService`/`JwtStrategy`: моки Google-профиля, проверка `findOrCreateByGoogleProfile`, валидация JWT payload.
- Файлы: `apps/api/src/**/*.spec.ts` (расположены рядом с тестируемым кодом — стандартная конвенция Nest).

### Frontend (Vitest + React Testing Library)
**⚠️ Vitest ещё не установлен** — это было согласовано как решение с самого начала проекта (см. `docs/plan.md`), но фактически пакеты не ставились ни на Этапе 1, ни позже. Первый шаг этого этапа — установка: `pnpm --filter web add -D vitest @vitest/ui jsdom @testing-library/react @testing-library/jest-dom`, создание `apps/web/vitest.config.ts` (окружение `jsdom`, `setupFiles` для `@testing-library/jest-dom`), добавление скрипта `"test": "vitest run"` в `apps/web/package.json` (сейчас там скрипта `test` вообще нет, несмотря на то что `CLAUDE.md` его упоминает).

- `CreateLinkForm`: валидация полей, успешный submit, отображение ошибок API.
- `LinksList`: рендер списка, копирование URL, удаление с подтверждением.
- `ClicksChart`/аналитика: корректный рендер при наличии/отсутствии данных.
- `auth.store`, `AuthGuard`: редирект неавторизованных, персист токена.
- Файлы: `apps/web/src/**/*.test.tsx`.

### E2E (Playwright)
- Новый пакет `apps/e2e` (или `apps/web/e2e/` — решить на этапе реализации в зависимости от того, насколько изолированной должна быть конфигурация от Vitest).
- Сценарий: логин → создание ссылки → переход по короткой ссылке (проверка редиректа) → просмотр страницы аналитики (клик появился).
- `playwright.config.ts`: запуск против локально поднятых `pnpm dev` + `docker compose up -d postgres`.

### Покрытие
- Цель >80%: `pnpm --filter api test:cov`, `pnpm --filter web test -- --coverage`.
- `apps/web/vitest.config.ts` — пороги покрытия (`coverage.thresholds`).

## Пошаговый план работ

1. Установить и настроить Vitest + RTL во фронтенде (см. врезку выше) — без этого шага `pnpm --filter web test` не существует.
2. Workflow-скрипт: параллельная генерация unit-тестов по списку файлов без покрытия; каждый сгенерированный тест верифицируется вторым агентом (реально запускается, проверяется, что не tautological — не просто `expect(true).toBe(true)`).
3. Agent view отслеживает статус параллельных прогонов во время генерации.
4. `/loop` гоняет `pnpm test` по всем воркспейсам до зелёного состояния, автоматически подправляя падения между итерациями.
5. Playwright E2E-сценарий пишется вручную (критичный код полного флоу — не полностью автогенерируется, требует осмысленного проектирования шагов).
6. Финальный прогон `pnpm test` + coverage-отчёт, проверка порога >80%.

## Ключевые файлы

| Файл | Назначение |
|---|---|
| `apps/api/src/**/*.spec.ts` | Backend unit-тесты |
| `apps/web/src/**/*.test.tsx` | Frontend component-тесты |
| `apps/e2e/*.spec.ts`, `playwright.config.ts` | E2E-сценарии |
| `apps/web/vitest.config.ts` | Пороги покрытия |
| `turbo.json` | Задача `test:e2e` |

## Верификация

- `pnpm test` (все воркспейсы) — зелёный.
- Coverage-отчёты показывают >80% на `api` и `web`.
- `pnpm --filter e2e test` (или эквивалент) проходит headless.

## Зависимости от предыдущих этапов

Этапы 2–5 реализованы — есть реальная бизнес-логика, которую тестируем.
