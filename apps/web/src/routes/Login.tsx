// Placeholder for Stage 4 (Google OAuth). Kept trivial on purpose - real content
// (Google sign-in button, redirect handling) lands with the auth implementation.
export function Login() {
  return (
    <div className="container py-16 text-center">
      <p className="text-muted-foreground">Login coming in Stage 4.</p>
    </div>
  );
}
